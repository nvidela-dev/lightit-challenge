Take into account ./Challenge Content.md
Take into account ./backend_requirements.md
Take into account ./frontend_requirements.md

Make 2 folders inside this repo, one for the frontend one for the backend
Serve the frontend at the / route of the express backend
We need docker with a volume for the database, and the capacitiy to run the backend.
We should have migrations with sample data
We need a makefile to control things like mailpit, the database, seeding, running the project, etc
